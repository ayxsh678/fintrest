# ── Web framework ───────────────────────────────────────
fastapi
uvicorn[standard]

# ── Data / finance ──────────────────────────────────────
yfinance
pandas
feedparser

# ── HTTP / utilities ────────────────────────────────────
requests
python-dotenv
cachetools

# ── Session memory (optional — degrades gracefully if absent) ───
redis

# ── Pydantic (pinned major to avoid v1/v2 compat issues) ────────
pydantic>=2.0,<3
